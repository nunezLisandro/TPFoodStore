// DataInitializer intentionally left blank.
// We removed the automatic insertion of test users so that users are only created
// via the registration endpoint. Keeping this file as a placeholder to avoid
// compilation issues if referenced elsewhere.

package com.example.foodstore.config;

public class DataInitializer {
}
